// temp for search, will delete it later
import Container from '@mui/material/Container';
import SearchForm from '../components/SearchForm';

export default function TempSearchPage() {
	return (
		<Container sx={{ marginTop: '20px' }}>
			<SearchForm />
		</Container>
  );
}